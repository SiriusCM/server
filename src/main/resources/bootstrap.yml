eureka:
  client:
    service-url:
      defaultZone: http://localhost:10000/eureka

spring:
  cloud:
    config:
      name: alone-1-1
      discovery:
        enabled: true
        service-id: config