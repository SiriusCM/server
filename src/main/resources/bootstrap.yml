eureka:
  client:
    service-url:
      defaultZone: http://10.77.38.129:10011/eureka

spring:
  cloud:
    config:
      name: alone-1-1
      discovery:
        enabled: true
        service-id: config